services:
  app:
    build: .
    container_name: avncodex-indexer
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./.env:/app/.env
    restart: unless-stopped
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////data/avn_index.db
      - PUID=1000
      - PGID=1000
